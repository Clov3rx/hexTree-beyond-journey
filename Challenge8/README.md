# Flag 8 - Returning Activity Results

## Solution 

`(callingActivity.getClassName().contains("Hextree"))` we can bypass this by create a new package containing Hextree 

### Key Concepts

1. Starting activities is not just a one-way communication. Activities can also return a result back to the caller when it was started with `startActivityForResult()`.



