# Exported Activity that processes a sequence of implicit intents

## Solution

The solution involves sending three implicit intents with specific
action values in the correct order. Once the sequence is completed,
the application returns the flag.